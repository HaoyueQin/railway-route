# .gitignore (master-v2: Rust + Tauri)

# 数据文件（过大，不入库，需自行获取）
data/timetable/
data/output/
data/station_coords.json

# Handover 文档（本地参考，不入库）
HANDOVER.md

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# 中间产物 / 缓存（可重新生成）
gui-test-screenshots/
.reasonix/
.zcode/

# 备份文件
*.bak
*.pyc
__pycache__/
data/w.db
.search_cache.sqlite
build/
dist/
*.spec
# Rust 构建产物
rust/target/
rust/gen/
# Rust 对拍中间产物（依赖 data/ 可重新生成）
rust/tools/*_baseline.json
